name : Release Drafter

on: 
  push: 
    bracches:
      - master
    pull_request:
      types: [Merged]
    permissions:
      contents: read

jobs:
  update_release_draft:
    permissions:
      contents: write
      pull_request: write
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v6
        env:
          GITGUB_TOKEN: ${{ serets.GITHUB_TOKEN }}